{
  "name": "chromatica",
  "version": "1.0.0",
  "private": true,
  "description": "Roda de cores interativa para explorar teoria das cores.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
